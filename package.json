{
  "name": "react-patterns-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "A growing collection of React best practices — each recipe done with as little client JS as possible (pure CSS, server-first). fit-text, parallax, accordion, slider, analytics, and more.",
  "license": "MIT",
  "author": "Oleksii Mazurenko",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oleksiimazurenko/react-patterns.git"
  },
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "turbo": "^2.10.6",
    "typescript": "^7.0.2"
  },
  "packageManager": "pnpm@11.17.0"
}
